import { useState } from "react";
import React from "react";
import { NotificationToast } from "../utils/NotificationToast";

export const ProbarComponentes = () => {
  return <NotificationToast text={"preba"} />;
};
