import { lazy } from 'react'

export const Buscador = lazy(() =>import('./Buscador'))
export const NotProduct = lazy(() =>import('./NotProduct'))
export const Results = lazy(() =>import('./Results'))
